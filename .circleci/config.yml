version: 2.1

jobs:
  build:
    docker:
      - image: cimg/android:2023.08
    resource_class: medium  # Changed to medium as it's more commonly available
    steps:
      - checkout
      - run:
          name: Check Java version
          command: java -version
      - restore_cache:
          key: gradle-{{ checksum "build.gradle" }}-{{ checksum  "TMessagesProj_App/build.gradle" }}
      - run:
          name: Download Dependencies
          command: |
            chmod +x gradlew
            ./gradlew dependencies
      - save_cache:
          paths:
            - ~/.gradle
          key: gradle-{{ checksum "build.gradle" }}-{{ checksum  "TMessagesProj_App/build.gradle" }}
      - run:
          name: Build APK
          command: ./gradlew :TMessagesProj_App:assembleAfatRelease --stacktrace --no-daemon
      - store_artifacts:
          path: TMessagesProj_App/build/outputs/apk/afat/release
          destination: apk

workflows:
  version: 2
  workflow:
    jobs:
      - build
